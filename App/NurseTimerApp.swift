import SwiftUI

// Item 1 scaffolding entry point. The real app (tabs, SwiftData container, etc.)
// is built in Milestone 2, which replaces this file.
@main
struct NurseTimerApp: App {
    var body: some Scene {
        WindowGroup {
            Text("NurseTimer")
        }
    }
}
