import SwiftUI

// Item 1 scaffolding entry point. The real Watch experience (Now view, task
// detail, actions) is built in Milestone 4, which replaces this file.
@main
struct NurseTimerWatchApp: App {
    var body: some Scene {
        WindowGroup {
            Text("NurseTimer")
        }
    }
}
