import XCTest

// Placeholder host for future app-layer unit tests. The authoritative scheduling
// tests live in the NurseTimerCore package (`swift test`).
final class PlaceholderTests: XCTestCase {
    func test_placeholder() {
        XCTAssertTrue(true)
    }
}
